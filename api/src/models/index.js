const reviewModel = require('./Review');
const localModel = require('./Local');
const userModel = require('./User');
const imageModel = require('./Image');

module.exports = {
  localModel, userModel, reviewModel, imageModel,
};
